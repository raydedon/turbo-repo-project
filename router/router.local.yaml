supergraph:
  listen: 0.0.0.0:4000
  path: /graphql
  introspection: true

cors:
  allow_any_origin: true
  allow_headers:
    - Content-Type
    - Authorization
    - Apollo-Require-Preflight
  methods:
    - GET
    - POST
    - OPTIONS

health_check:
  path: /health
